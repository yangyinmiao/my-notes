# expense-agent 后续优化方向

> 记录时间：2026-05-13
> 状态：待实现（暂不开发）

## 1. 多币种支持 ⭐⭐

**技术方案：**
- 发票表加 `currency` 字段 + `exchange_rate` 字段
- 识别时 GPT 提取币种
- 接入 `exchangerate-api.com` 实时汇率
- 报表统一换算为 CNY 展示

---

## 2. 报表智能建议

> 原始消息被截断，待补充完整方案

---

## 3. 发票原件预览优化（已完成）

- PDF：内嵌 iframe 预览 + 右上角「下载原件」按钮
- 图片：正常图片预览 + 下载按钮
- 需重启 Vite dev server 使 proxy 规则生效

---

## 相关文件

- 项目路径：`~/Documents/Development/PersonalProjects/expense-agent`
- 前端：`frontend/`（Vite + React）
- 后端：FastAPI + PostgreSQL + Redis + MinIO + Celery
